[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pydelog"
version = "1.0.0"
description = "Debug and logging utility with colored output and timers."
authors = [{name="Sajan Saji", email="isajansaji@gmail.com"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/sajansaji/pydelog"
